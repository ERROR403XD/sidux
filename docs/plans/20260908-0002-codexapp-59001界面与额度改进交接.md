---
title: CodexApp 59001 界面与额度改进交接
created: 2026-09-08
status: 开发中
---

# 范围与边界

用户授权针对 59001 开发候选持续分批开发。源码 `/home/Code/codexapp`，分支 `feature/0.2.11-development`。生产 5900 不在本次范围。候选容器 `codexapp-multi-account-dev`，独立卷 `codexapp-multi-account-dev-home:/codex-home`；不得挂载生产 `/root/.codex`。

# 任务清单

- 导航：自动主题太阳+A；新建移到 Git 右侧；仅会话路由继承实际项目目录，其他入口新建无项目会话；折叠侧栏仅保留展开按钮。
- 文案：删除设置说明长句、重复额度窗口/剩余/正常就绪字样。
- 额度：常驻更新时间和重置时刻；两行显示、按已用比例每 20% 蓝绿黄橙红；剩余轨道保持可辨认；全局时区生效；当前账号描边。
- 激活：手填时分；移除独立时区，计划使用全局设置时区。
- 等级：手动刷新强制读取所有账号实时额度与等级；后台复用有限频率刷新，不依赖旧 token 等级覆盖实时等级。
- 验收：定向测试、构建、浅深色与移动视口、候选部署、性能检查；分批提交。

# 当前进度

已核对源码干净、候选 59001 容器运行、Vault NFS 挂载。导航修改进行中，尚未部署。原候选镜像 `codexapp-multi-account-dev:ui-0.2.11`。中断后先检查 git diff/status、本文件及候选状态，再继续未验收项。

# 回滚与验收要求

候选替换前通过 `scripts/check-codexapp-idle.cjs`，保留旧镜像；不删除命名卷，不发送耗额度测试消息。所有运行行为必须区分受控 fixture 和真实候选。每批提交和最终截图/测试结果在本文件补充。

# 阶段检查点（部署前）

- `b662c6d` 已提交：实时额度等级优先、读状态不再用旧 JWT 套餐覆盖、网关保留 windowMinutes。新增回归测试抓到并修复真实等级回退与窗口名退化。
- 定向测试6文件58项通过；完整构建和 CJS help 退出0；最终界面小修后再构建打包。
- `output/0211-refine/ui.cjs` 六组视口/主题通过；`edge.cjs` 验证 UTC 全局切换和保存后刷新、无项目及自动化新建、无效小时阻止提交、错误常驻。fixture 无模型投递。
- 4173 由当前源码启动，独立 `CODEX_HOME=/tmp/codexapp-59001-ui-home`，未复用生产认证。进程保留供复测。
- 性能：4173真实空白主页 profiler，14.6 KB API，warnings=[]，thread/list、skills/list、rateLimits/read、provider-models 各1次；最慢 provider-models 275.3ms。不是有大量会话/账号的压力测量。账号手动刷新串行、每账号一次，后台沿用5分钟TTL/已有轮询；未增加后台定时器。每次刷新仍返回全账号列表，大账号池总响应量随账号数平方增长，当前双账号 fixture 验证无并发扩散。
- 时区延用既有浏览器全局偏好：显式修改会写服务器计划；首次打开设置会对齐旧计划时区。多浏览器最后一次保存/对齐生效，无另设时区。不会发送激活消息。

下一步：最终构建/打包 → 为旧候选保留可启动回滚容器 → idle-gated 脚本替换59001 → 真实候选浅深界面与只读额度时间校验 → 提交最终交接。生产5900 MainPID仍3002098。
