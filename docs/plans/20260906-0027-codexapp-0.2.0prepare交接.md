---
title: CodexApp 0.2.0 prepare 交接
created: 2026-09-06
updated: 2026-09-06
tags: [开发, CodexApp, 发布]
status: prepare 完成；生产有活动回合，未切换
---

# 0.2.0 prepare 交接

用户授权 prepare 当前版本。本次准备 0.2.0，应用基线 `67766b2`（含 Goal 模型选择），发布脚本补齐后代码提交 `8542ec5e58c8`。后续文档提交不改变该发布产物。

- 发布目录：`/home/docker/codexapp-releases/codexapp-0.2.0-8542ec5e58c8-20260906-221809`。
- 应用包：`/home/docker/codexapp-releases/codexapp-0.2.0-8542ec5e58c8-20260906-221809/codexapp-0.2.0.tgz`。
- 应用包 SHA-256：`737c2d9921251456ca2e29d94d743cb3027fd3304ba60c1a51a6ca2013156f4a`。
- 固定 CLIProxyAPI 7.2.152 在发布目录的 `api-proxy-component/`，含 manifest、LICENSE 和校验后的二进制；它独立于应用 tgz，搬运发布版本时需保留整个目录。
- `latest-prepared` 已指向上述目录，旧发布保留。

## 本次补齐

原 prepare 仅准备应用包，反代组件路径只适用于容器。本次让 prepare 复制经固定 manifest 校验的组件；未来 activate 生成的 drop-in 使用该版本自己的 `CODEXAPP_API_PROXY_BINARY`。check 在访问生产前验证组件，缺失或损坏必须失败。prepare 本身不安装/修改生产 drop-in。

新增损坏组件回归发现命令替换中的 Bash errexit 不能独自保证中止，已显式传播校验失败。两个发布脚本用例通过，覆盖组件损坏、目标环境路径与既有 activate/rollback fixture。没有对真实生产执行 activate/rollback。

## 验证与边界

- 全量测试 42 文件、300 项通过；构建、vue-tsc 和 CLI help 通过。
- 发布目录 dist/dist-cli 共 30 文件与运行中的 59001 逐字节哈希一致，因此沿用既有 API/Goal UI 与真实客户端验收。
- CJS require 入口与实际 node-pty spawn/输出/退出码 0 通过。npm 提示部分安装脚本未运行，但实际原生 PTY 验证成功，没有盲目放开所有安装脚本。
- 使用全新临时 HOME/CODEX_HOME 在 127.0.0.1:59015 启动发布包；API 管理接口确认组件已安装、出口默认关闭，无 key 的 /v1/models 返回 401。已停止临时服务并清理临时状态；未复制生产或候选认证。
- 组件 SHA-256：`fc9b13ecda255129b7c452a8c0a84bd0669de0b546d0919f5566cff5397c0c23`。
- `check latest` 退出 3：activeTurns=1、queued=0、pendingApprovals=0、apiConnections=0、apiActiveRequests=0。当前不能认为生产空闲。
- 5900 保持 0.1.90、PID 2722536，未重启；59001 保持运行。未推送、合并或切换生产。

性能审计：应用产物与已验收候选一致，无运行时 API/UI 性能变化。新增校验仅在 prepare/check/cutover 读取一份固定大小二进制并计算哈希，没有请求循环或后台轮询；本次 check 约 0.18 秒。没有新增长时压测。

原始日志与机器证据：`/home/Code/codexapp/output/0200-prepare/`；版本化摘要见 [prepare 验证](evidence/0.2.0-prepare-verification.json)。

## 后续外部终端切换

仅当用户决定上线、结束活动任务并从独立终端操作时使用。当前 prepare 授权不等于已授权本会话执行 activate；不要绕过忙碌检查。显式指定发布目录，避免未来 latest 改变。

```bash
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh check /home/docker/codexapp-releases/codexapp-0.2.0-8542ec5e58c8-20260906-221809 &&
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh activate /home/docker/codexapp-releases/codexapp-0.2.0-8542ec5e58c8-20260906-221809 --confirm-idle
```

切换后以 status、页面版本、Goal 与 API 出口组件状态为准；生产账号池与候选独立，需按实际生产账号启用出口、创建自己的 key。若切换后需要回滚，先共同空闲，再用既有 rollback 流程恢复代码与 drop-in，保留合法轮换后的认证和 key 撤销状态。
