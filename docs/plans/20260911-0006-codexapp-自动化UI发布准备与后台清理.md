---
title: CodexApp 自动化 UI 发布准备与后台清理
created: 2026-09-11
updated: 2026-09-11
status: prepare完成；已清理4173；空闲门禁未通过；未切换
---

# 发布准备与后台清理

本次覆盖 0004 的发布目录交接，包含 [0005 自动化界面与会话完成蓝点调整](20260911-0005-codexapp-自动化界面与会话完成蓝点调整.md) 及启停后保留行顺序、刷新再排序的补充。

## 发布包

- 源码：`d31d40e289ed`，`feature/0.2.17-development`；prepare 前工作树干净。
- 版本：0.2.17。
- 精确发布目录：`/home/docker/codexapp-releases/codexapp-0.2.17-d31d40e289ed-20260911-125921`。
- 命令：`bash scripts/codexapp-release-switch.sh prepare`，成功。
- 27 个 dist/dist-cli 文件与本地构建一致；候选 59001 的 15 个 HTTP 资源及入口引用与发布包一致，CLI 与候选相同。
- CLI SHA256：`4efcc96c9626eb1201c2e12b686fca082fc57ad4b904e4a141f5d53e70463263`。
- tarball SHA256：`edb4c9a991c656778cfc2d647ec01015df571c564dbef67fd49194dc765df93a`。

## 验证

`PREPARED_RELEASE=/home/docker/codexapp-releases/codexapp-0.2.17-d31d40e289ed-20260911-125921 node output/0217-prepare-ui/verify-prepared.cjs` 通过，执行包内 CJS require 帮助并验证 `node-pty.spawn` 真实 shell 返回 `PREPARED_PTY_OK`、退出 0。

CJS 实际调用：

```bash
PREPARED_RELEASE=/home/docker/codexapp-releases/codexapp-0.2.17-d31d40e289ed-20260911-125921 node -e "process.argv=['node','codexapp','--help'];require(process.env.PREPARED_RELEASE+'/dist-cli/index.js')"
```

隔离 59015 运行旧生产包，再在同一浏览器缓存/源地址切到新包：入口 no-store，无 ETag/Last-Modified；条件请求返回 200；带 hash 资源 immutable；旧入口资源 404 且非 SPA；无 key 的 `/v1/models` 返回 401。真实页面刷新命中新资源缓存，无页面 JS 错误。命令见 `output/0217-prepare-ui/cache-migration.cjs`，使用独立 migration-home，不加载真实账号。

浏览器 URL `http://127.0.0.1:59015/#/settings`，1440×1000 浅色；包版本、设置结构、资源迁移断言通过，等待 2.3 秒截图。原图 `/home/Code/codexapp/output/playwright/0217-prepare-ui-cache-migration.png`。

![真实缓存迁移](/home/Code/codexapp/output/playwright/0217-prepare-ui-cache-migration.png)

## 后台清理

4173 确认为本次任务启动：cwd `/home/Code/codexapp`，`CODEX_HOME=/tmp/codexapp-automation-ui-home`，Vite PID 1338417，启动链 1338360 → 1338408 → 1338409 → 1338417。原工具终端 processId=22618。

核对原生终端元数据后，通过 `/codex-api/process-activity/terminate` 精确停止该终端。确认 4173 无监听、启动链全部退出、后台终端记录消失。本次缓存测试自行清理 59015 并确认无监听。最终本会话后台终端列表为空。没有批量终止 node/pnpm/浏览器，也没有终止其他活跃回合。

这替代 0005 的“4173 留在运行”状态：进入发布交接后已停止 4173。5900、59001、5173 未停止。

## 空闲门禁与后续操作

清理后对精确发布路径执行 check，退出 3：

```text
liveExecutions=2
activeTurns=2
queued=0
pendingApprovals=0
apiConnections=1
apiActiveRequests=1
backgroundThreads=skipped-busy
pages=2
```

全局后台枚举因忙碌未完成，不得将 skipped-busy 或 loadedThreads=0 解释为全局后台为空。当前没有执行 activate、重启生产、复制认证或切换账号。

生产仍为 `/home/docker/codexapp-releases/codexapp-0.2.17-3d924ac039b7-20260911-010224`，MainPID=796110，active，NeedDaemonReload=no。

本对话结束、其他活跃回合/API 请求完成，并关闭 CodexApp 标签页后，从独立主机终端复查：

```bash
cd /home/Code/codexapp
bash scripts/codexapp-release-switch.sh check /home/docker/codexapp-releases/codexapp-0.2.17-d31d40e289ed-20260911-125921
```

仅当检查通过、独立 CLI 工作也已结束，且用户授权实际切换后：

```bash
bash scripts/codexapp-release-switch.sh activate /home/docker/codexapp-releases/codexapp-0.2.17-d31d40e289ed-20260911-125921 --confirm-idle
```

本轮仅准备切换，不代表已授权或已完成 activate。切换后的账号和真实响应属于后续验收。

证据：`/home/Code/codexapp/output/0217-prepare-ui/`，包含 prepare.log、prepare-verification.json、verification.log、cache-migration.json、cleanup.json、terminal-final.json、check.log 及复现脚本。交接文档在 prepare 后提交，不改变已验收发布包。
