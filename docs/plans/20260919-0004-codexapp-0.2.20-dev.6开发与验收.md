---
title: codexapp 0.2.20-dev.6 开发交接
created: 2026-09-19
tags: [开发, CodexApp, 发布, Sidux]
status: dev.6 三项改动已完成并验证，未 prepare
---

# codexapp 0.2.20-dev.6 开发交接

## 1. 基线与分支

- 工作树：`D:\Code\Sidux`（Windows 本机副本，取代 NAS 轮换流；本副本为纯文件拷贝，无 Git 元数据）。
- git 恢复方式：GitHub 远端（`https://github.com/ERROR403XD/sidux.git`）`main@186fbbb` 之上以基线提交 `ef8fa06` 导入 dev.5 工作树（package.json 0.2.20-dev.5）。NAS 副本里的 dev.1–dev.5 真实提交（`e33c1a3` / `fc5b4f9`）在本机不可达，未带回；如需拼接历史，后续从 NAS `.git` fetch 后 graft。远端独有文件（`.gitignore`、两篇 RELEASE 文档、一篇测试文档）已在基线提交中恢复。
- 开发分支：`feature/0.2.20-dev.6`；`package.json` 版本已提升为 `0.2.20-dev.6`。

## 2. dev.6 改动内容（三项，均按任务单提交）

### 2.1 API key 账号下拉框固定宽度溢出（`f4487eb`）

- 现象：账号下拉框在一定窗口宽度范围内固定 260px，特定宽度（约 806px）下溢出行容器。
- 根因：`src/style.css` 的 `.api-proxy-inline-policy .app-select.api-proxy-key-account` 带 `min-width: 260px` 硬下限；key 行双列网格的左列被右列操作区（或更宽的按钮文案、或容器查询不生效的环境）压到 260px 以下时，下拉框钉在 260px 溢出。Playwright 强制双列复现：溢出最高 260px。
- 修复：`min-width: min(260px, 100%)`——有空间时保持 260px 稳定宽度（原设计意图），不足时随列收缩。
- 验证：中英文、375–1280 全宽度、菜单展开态、明暗主题、强制双列模拟，均无溢出；806px 明暗截图 `output/playwright/dev6-select-fixed-806-*.png`。

### 2.2 等待区消息自动发送后残留（`2ac626f`）

- 根因：`codexapp/queue/changed` 推送不带令牌且断线不重放；SSE/WS 重连的 `ready` 分支不刷新队列状态，浏览器离开期间发生的自动发送无法反映到页面，残留排队行点击「删除」命中服务端 409（`该消息已发送或已移除`）。
- 修复：客户端 `ready`（重）连接分支补 `refreshQueueState()`（`useDesktopState.ts`）；服务端 `BackendQueueProcessor.mutate` 对已结算行的 `remove`/`abandon` 改为幂等成功（返回最新状态 + `delivered` 回执），其余操作维持报错；`reconcile`/`steer` 行为不变。
- 验证：两个新回归测试（ready 重同步；排空后幂等删除）+ 全量单测；行为手册见 `tests/chat-composer-rendering/persisted-idle-queue-recovery.md` dev.6 节。

### 2.3 自定义连接推理强度（`404b91c`，含探测兜底）

- 背景：自定义连接的模型目录通常不带 `supportedReasoningEfforts`，`efforts` 恒为空 → 选择器 N/A、请求链路剥离 reasoning 参数；DeepSeek 等 OpenAI 兼容上游实际支持 `reasoning_effort`。
- 实现：连接级声明 `reasoningEfforts`（minimal/low/medium/high/xhigh，规范化去重定序）。声明是本地字段：不参与探测摘要、免复测保存；存储层保留原始探测 models，`get/active/snapshot` 读取时合并声明（清空声明即恢复目录）。声明后整条既有链路生效：选择器列档位、`turn/start` 保留 effort、运行时/API 出口 sanitize 不再剥离、协议桥完成 `reasoning.effort ↔ reasoning_effort` 互转。UI 在连接表单加芯片行（aria-pressed，明暗主题）。
- 探测兜底：`test()` 对两个协议各加一个 16-token 变体探针（plain + 带 `reasoning_effort`/`reasoning.effort`），按解析出的 wireApi 归类：错误点名 reasoning 参数 → `rejected`；变体正常返回 → `accepted`（不拒绝 ≠ 确认生效）；其余 → `unknown`。`rejected` 时服务端保存钳制声明为空、读取合并跳过声明，UI 禁用芯片并提示——选择器回到与未声明一致的禁用态。探针不参与摘要；旧状态文件无该字段按 `unknown` 处理。
- 验证：store 生命周期测试 + 探针分类测试（rejected 钳制 / accepted / unknown）+ HTTP 透传集成测试（声明后上游带 `reasoning_effort`，未声明剥离）+ Playwright UI 16/16（含拒绝型上游全流程，`output/playwright/verify-dev6-efforts-ui.cjs`，截图 `dev6-efforts-form-*.png`、`dev6-efforts-rejected-*.png`）。

## 3. 测试与验证汇总

| 项 | 结果 |
| --- | --- |
| `vue-tsc --noEmit` | 通过 |
| `vitest run`（全量） | 949 通过 / 31 失败；31 项失败与 dev.5 基线在此 Windows 主机上完全一致（缺 Codex CLI、POSIX 0600 权限断言等环境性失败，已用 stash 对照基线复核） |
| `vitest run --config protocol-bridge/vitest.config.ts` | 70/70 通过 |
| 新增单测 | +6（队列重同步、幂等删除、声明生命周期、HTTP 透传、探针拒绝钳制、探针接受/不确定） |
| Playwright | 下拉框扫描、强制双列、推理强度 UI（含拒绝型上游，16/16）共 4 个脚本，`output/playwright/`；全部通过 |

## 4. 性能审计

- 代码路径分析：修复 2 在每个 SSE/WS（重）连接事件上增加一次 `GET /codex-api/thread-queue-state`（事件驱动、带 generation 单飞守卫，非轮询；队列载荷只含未发送行，常规为空）。服务端幂等删除在已发送场景反而省一次失败往返。功能 3 无新增请求；读取时合并仅在 get() 上做一次浅拷贝与小集合分配，量级远小于每请求的 body 解析。探针仅在「测试连接」时发生，每协议多一个 16-token 变体请求（2 个/次），与既有端点探针并行发出，不增加测试等待时间，运行时零影响。修复 1 为纯 CSS。
- 产物：未重新构建 dist（本回合未要求打包）；源码级无新增轮询、无启动期阻塞、无无界扇出、无缓存失效面变化。
- 未测量：真实 DeepSeek/上游端到端延迟与吞吐（本机无真实凭据），需按手册真机复验。

## 5. 未决事项

1. NAS `.git` 元数据未带回（本机不可达）；如需完整 dev 线历史，从 NAS 侧 fetch。
2. 本机 Windows 环境的 31 项既有单测失败（缺 Codex CLI、文件权限断言）与本次改动无关，如需清零需装 Codex CLI 或将权限断言标注平台条件。
3. 4173 验证服务器以 `CODEX_HOME=output/codex-home-4173` 独立实例运行（detached，`output/playwright/start-vite-4173.cjs` 可重建）；原进程已被替换，5173/5900 未触碰。
4. dev.6 未 prepare；激活/发布仍按 NAS 轮换约定执行。

## 6. 回滚

- 三个改动相互独立，`git revert f4487eb / 2ac626f / 404b91c` 即可单独回退；`reasoningEfforts` 字段对旧代码无害（会被忽略），无状态迁移负担。
