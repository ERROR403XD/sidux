---
title: CodexApp 刷新右对齐与重新发布准备
created: 2026-09-11
updated: 2026-09-11
status: GitHub已替换；prepare完成；未切换
---

# 刷新右对齐与重新发布准备

插件工具栏改为搜索、筛选、刷新，刷新按钮通过末尾布局和 margin-left:auto 与下方插件/空态区域右边缘对齐。窄窗口换行后仍靠右；保留 AppButton 主题样式及原请求处理，无新监听器或网络调用。源码提交 c4fdff1，发布说明同步提交 1b96562。

## 验证

- 本地类型检查、前端构建通过；公开树完整构建通过，685 单测通过、1 opt-in 原生测试跳过。
- Playwright 使用合成插件响应，在 http://127.0.0.1:4173/#/skills?tab=plugins 检查 1440×1000、768×1024、375×812 的深浅色六组情况。右边缘差为0，每次点击1次刷新请求，无页面错误。首次截图超时，关闭浏览器后重跑完整矩阵通过。
- 截图绝对目录 /home/Code/codexapp/output/playwright/，文件 refresh-align-1440-light.png、refresh-align-1440-dark.png、refresh-align-768-light.png、refresh-align-768-dark.png、refresh-align-375-light.png、refresh-align-375-dark.png。
- 性能：无新增网络/监听器；公开主入口仍815.25 kB，gzip258.55 kB，大分块提示保留。

## 替换 GitHub v0.2.17

- PR #4 https://github.com/ERROR403XD/codexapp/pull/4 已合并，main与标签目标951a82e1e95e87ec66d0b1d479889e32b1541269。
- Release https://github.com/ERROR403XD/codexapp/releases/tag/v0.2.17 沿用版本号，按用户要求更新标签并替换安装包、SHA256SUMS及说明。
- 新公开包SHA256：85c6985536c9a782142e43f9b1b56706b5d467f8cbf519a208cb0725aa8af478。回下载校验及逐字节比较通过。
- 815源码文件及37压缩包条目隐私检查无命中；个人配置、内部记录及截图不公开。公开安装包隔离安装、CJS CLI帮助及真实PTY通过。未发布npm registry。

## 本地 prepare 与切换门禁

最新准备目录：`/home/docker/codexapp-releases/codexapp-0.2.17-1b9656206ceb-20260911-193658`。

27个构建文件一致，59001 CLI一致，CJS帮助和PREPARED_PTY_OK通过。代理组件固定版本摘要检查通过。59001仅同步前端assets及入口，未重启；入口引用与资源内容一致，服务器动态外观注入保留。

59015隔离同源浏览器从旧包迁移新包：入口no-store、无ETag/Last-Modified、资源immutable、旧资源404、未授权代理401、刷新缓存正确，无页面错误。临时进程退出，4173和59015均清理，5173未触碰。

最后一次check：liveExecutions=4，activeTurns=4，queued=0，pendingApprovals=0，apiConnections=1，apiActiveRequests=1，backgroundThreads=skipped-busy。门禁未通过，未执行activate，5900仍为原运行版本。忙碌数字是检查时快照，切换前必须重查。

后续获准切换时先运行：

```bash
bash scripts/codexapp-release-switch.sh check /home/docker/codexapp-releases/codexapp-0.2.17-1b9656206ceb-20260911-193658
```

不要沿用旧935deb准备目录。只在检查通过并排除直接宿主CLI活动后执行已有activate流程。回滚退回先前代码，不回滚真实认证、会话和已发送消息。本次证据在output/public-0.2.17-refresh/，不公开。
