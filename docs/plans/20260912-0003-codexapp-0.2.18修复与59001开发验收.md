---
title: CodexApp 0.2.18 修复与 59001 开发验收
created: 2026-09-12
updated: 2026-09-12
tags: [开发, CodexApp, 验收]
status: 隔离开发候选已运行；未发布
---

# CodexApp 0.2.18 修复与 59001 开发验收

## 范围与实现

本轮用户授权在 59001 开启 0.2.18，修复 0.3 展望配套小版本计划中的 R01/R02，增加会话功能提示关闭，并将 S05 做成轻量“已保存”。随后另做 0.3 UI 草案。

| 项目 | 结果 |
| --- | --- |
| R01 反馈入口 | 旧邮箱和 mailto 构造已移除。现有入口打开共享 AppDialog，预览/编辑/复制最小报告；用户可自行打开当前项目 Issues |
| 采集范围 | 只导出版本、时间、页面类别、主题、界面语言、视口、错误类别和 HTTP 状态。最多 12 条诊断；不读页面文字或存储，不导出原始错误、路径、URL、会话正文 |
| R02 额度文案 | QUOTA_EXHAUSTED 保持不变，文案改为查看实际额度与恢复时间、恢复后再试；保留认证/网络等分类及原调度行为 |
| 功能提示关闭 | 历史操作、队列功能错误和命令错误增加 ×，只收起本次提示。再次发生错误仍可显示；不修改持久失败回合或审批状态 |
| 命令反馈 | `/copy` 等直接命令的功能错误有独立输入框上方提示；按会话上下文隔离，旧会话异步结果不会写到其他会话 |
| S05 | 现有 localStorage 写入成功后显示轻量“已保存”，清空后消失；失败显示“保存失败 / 复制草稿”，复制不可用时选中输入。未增加存储动作、网络请求或保存流程 |

草稿文字复制不备份附件二进制；附件仍沿用已有持久化引用。同步存储没有额外引入“保存中”的人工延时或动画。

## 开发环境

- 分支：`feature/0.2.18-development`，从含 0.2.17 及 0.3 初稿的当前工作树建立。
- 候选：`http://192.168.50.46:59001/`，当前构建 `dist-cli/index.js`，独立 `CODEX_HOME=/home/docker/codexapp-dev-0218/codex-home`。
- 4173：当前仓库 Vite，独立 `CODEX_HOME=/home/docker/codexapp-dev-0218/verification-home`。
- 两个环境均未复制生产认证、会话或账号；目前候选为未登录的新开发环境。未调用真实模型。
- 5900 原服务 PID 3840130 保持不变；本轮没有发布、合并或生产切换。

启动命令：

```bash
CODEX_HOME=/home/docker/codexapp-dev-0218/verification-home pnpm run dev --host 127.0.0.1 --port 4173
CODEX_HOME=/home/docker/codexapp-dev-0218/codex-home node dist-cli/index.js --port 59001 --strict-port --no-password --no-open
```

运行日志在 `/home/Code/codexapp/output/0218/vite.log` 和 `candidate.log`。后续重建前先确认开发候选空闲；此运行方式直接读取仓库 dist，不是生产发布目录。

## 验证

- `pnpm run build`：通过 Vue 类型检查、前端构建和 CLI 构建。
- `pnpm exec vitest run`：688 项通过、1 项 opt-in 跳过。补充报告隐私/预览/边界和额度分类回归，保留 fetch 安装失败保护测试。
- 既有 `useUiLanguage.test.ts` 缺 document 事件桩，且仍预期 Worktree 不翻译；对照当前源码后修正测试，产品翻译逻辑未变。
- `node -e "require('node-pty');console.log('node-pty spawn:',typeof require('node-pty').spawn)"` 输出 `node-pty spawn: function`。
- `node dist-cli/index.js --help` 出口码 0；CLI 是 ESM，使用公开 CLI 入口烟测。未额外重测真实 PTY 创建。
- 4173 浏览器：草稿刷新恢复；模拟 QuotaExceededError 后内容保留及复制入口；技能接口合成 500 后从实际反馈入口打开报告，检查不含草稿或错误原文；明暗主题通过，未出现页面异常。
- 4173 功能提示：新会话选择 `/copy` → 功能提示 → 点击 × → 提示消失且草稿保留 → 再次操作提示重新出现，明暗主题通过。
- 59001 实际构建：同一命令提示关闭、草稿保存与刷新恢复通过，1440×1000、375×812、768×1024 明暗主题无横向溢出。
- 候选 idle 检查：activeTurns、queued、pendingApprovals、apiConnections、apiActiveRequests、backgroundThreads 均为 0。

浏览器使用 Playwright CJS + `/snap/bin/chromium`：当前没有 Browser Use 工具，存储失败和 API 合成错误需浏览器注入/路由拦截；未更改真实账号配置。此任务未改变发送/回复、失败回合或认证执行语义，未重跑认证/provider Docker 矩阵。

原始脚本、JSON 和日志位于 `/home/Code/codexapp/output/0218/`，分别为 `ui.cjs/ui.json`、`notices.cjs/notices.json`、`candidate-ui.cjs/candidate-ui.json`、`all-tests.log`、`build.log`、`idle.log`。跨真实会话和队列编辑草稿的完整手工步骤已补入 tests，本次自动浏览器验证覆盖新会话刷新，没有声称完成全部历史/队列真实场景。

## 性能审阅

有效首页 profile：thread/list=1、thread/read=0、skills/list=1、额度读取=1、模型目录=1；总 API 15.1 KB，warnings=[]，无持续 Loading threads。报告：`output/playwright/browser-runtime-profile-home-2026-09-12T00-15-17-872Z.json` 及同名 trace/screenshot。

本轮提示和草稿状态仅响应已有状态，没有新增轮询、API、逐会话读取、Git/文件扫描或缓存失效。报告只在点击时读取小型界面元数据及最多 12 条诊断；原有 20 条诊断上限保留，删除了页面/存储遍历。草稿继续使用原 watcher 与同步存储，未优化既有大附件序列化成本。

主入口构建 816.28 KB / gzip 258.87 KB。仍有大分块提示；本次没有长会话渲染或真实项目规模下的计时结果。新增工作台草案无 API/模型调用，不代表未来工作台实现的性能。

## 截图

下图分别来自 `http://127.0.0.1:4173/#/skills?tab=skills` 和 `http://127.0.0.1:59001/#/`，等待 2.3 秒后拍摄。完整截图绝对路径前缀为 `/home/Code/codexapp/output/playwright/`，归档副本字节一致。

![最小报告，1440×1000 深色](assets/0.2.18/0218-report-dark.png)
![功能提示与已保存，1440×1000 浅色](assets/0.2.18/0218-candidate-1440-light.png)
![手机功能提示，375×812 深色](assets/0.2.18/0218-candidate-375-dark.png)
![平板功能提示，768×1024 浅色](assets/0.2.18/0218-candidate-768-light.png)

## 后续

[0.3 活跃项目工作台三版草案](20260912-0004-codexapp-0.3活跃项目工作台三版草案.md)已完成可点击原型。本轮没有把工作台改进并入实际 App 路由，待用户比较方向后细化。


## 后续界面精简与统一时间控件

按照用户两次截图反馈，四处统一为与自动化每日时间相同的带时钟图标原生 `type=time` 控件：POST 免打扰、Telegram 免打扰、每日账号激活、自动化每日时间。不是早期讨论的纯文本框；展示格式由浏览器地区设置决定，保存仍为 HH:mm。

移除了设置及账号弹出区的“尚无账号，请从此面板添加。”、无账号时的模型偏好差异提示、关于页工作树，以及自动化正常运行的调度器文案。异常、初始化和交接中的调度器提示继续保留。

免打扰时间框固定为 96px，激活时间为 104px；开关标签间距 12px，开关与时间组间隔 20px。窄屏自然换行。原生选择器保持可用，中文冒号及全角数字粘贴通过共享解析统一为标准时间；非法粘贴不覆盖已有值、不保存，自动化空时间也不能默默变成零点。

验证：`pnpm run build` 通过；时间解析、激活调度、Telegram 配置及翻译相关 4 文件 32 项测试通过。4173 中文覆盖 1440×1000、375×812、768×1024 明暗主题；59001 英文覆盖 1440×1000、375×812 深色，均通过原生控件类型、开关不重叠、窄宽度、零横向溢出、保存刷新恢复、中文冒号/全角数字归一化和非法粘贴无保存请求的断言。实际账号弹出区也核对空提示已移除。浏览器页面异常为零。

浏览器读写均使用合成配置和路由拦截，未触发真实通知或账号激活。32 项为本次相关测试，并非重复全量回归。原始证据位于 `/home/Code/codexapp/output/0218/time-ui.json`、`time-candidate-ui.json`、`time-tests.log`、`time-build.log`。

性能：不新增 API、轮询、全局监听或定时器；共享组件只处理自身原生事件，激活保存沿用原有 400ms 合并逻辑。4 个通知输入框和激活/自动化输入按现有页面生命周期挂载。主入口 816.68 KB / gzip 259.07 KB，相对上一版增加约 0.40 / 0.20 KB；未重测首页或长会话性能。未更改服务端运行逻辑。

下图来自 `http://127.0.0.1:59001/#/settings`，1440×1000、深色、中文浏览器地区，等待 2.3 秒后截取实际控件区域。原始绝对路径分别为 `/home/Code/codexapp/output/playwright/0218-time-quiet-detail.png` 与 `0218-time-activation-detail.png`。

![免打扰统一控件与间距](assets/0.2.18/0218-time-quiet-detail.png)
![每日激活统一控件](assets/0.2.18/0218-time-activation-detail.png)


## 自动化每天运行支持多个时间

新建/编辑自动化的“每天”改为与账号自动激活一致的添加/移除时间列表，沿用带时钟图标的原生控件；最多 24 个时间，保存排序去重，空列表和非法时间拒绝保存。自动化详情显示选中的时刻。

单时间继续保存原来的单条 RRULE；多个时间在同一 rrule 字符串中保存为多行 RRULE，每行对应一个准确的每日时刻。服务端只接受最多 24 个可解析的每日时刻并合并其 next/previous 结果，不把小时和分钟交叉配对。仍是一个自动化定义和原有执行历史，不复制出多个任务。TOML 字符串回读保留多行规则；带其他日历约束、无法由每日列表完整表示的旧规则保持高级编辑，避免丢失约束。

验证：构建、类型检查、CLI `node dist-cli/index.js --help` 通过；3 个相关测试文件 43 项通过。覆盖 08:15/18:45 精确执行、去重、旧单规则和列表规则、TOML 回读、next/previous、空/非法/超限、24 时刻及 DST 跳过/重复小时。

4173 浏览器完成新建保存、刷新编辑、删除时间再保存；1440×1000、375×812、768×1024 浅深色均无横向溢出。59001 在 idle 检查为零后仅重启本次独立候选，实际创建 PAUSED 测试自动化，完成真实文件读取到编辑器再保存的验证；精确删除测试 ID，并移除本次临时目录，没有触发模型或调度执行。5900 未重启。

原始证据：`output/0218/multitime-ui.json`、`multitime-tests.log`、`multitime-api.log`、`multitime-candidate-ui.log`、`multitime-build.log`。性能审阅：前端添加/移除只更新本地数组，仍一次保存请求；不新增轮询、线程正文或文件扫描。服务端 next/previous 最多合并 24 个有界日历规则，沿用既有时区/DST 逻辑；已验证 24 个时刻连续求 next。主入口 818.19 KB / gzip 259.72 KB；未做大量自动化并发运行的负载测试。

下图为 `http://127.0.0.1:59001/#/automations` 实际编辑弹窗，1440×1000、深色，等待 2.3 秒后截取时间列表。原图绝对路径 `/home/Code/codexapp/output/playwright/0218-multitime-candidate-detail.png`。

![自动化每日多个时间](assets/0.2.18/0218-multitime-candidate-detail.png)


## 追加：侧边栏状态标签筛选

用户继续开发 0.2.18，新增项目/会话区顶部的“活跃中、未读、中断”标签。采用能力认证 tag 类似的紧凑外观，单选，再次点击取消；选中标签白字、白边框。无“全部”标签、无常驻说明文字。

筛选仅隐藏会话和无匹配会话的项目，保留项目归属、固定区、会话区、当前排序与折叠状态，不创建独立结果列表。内部始终保留完整 groups 和 pin 数据，避免被隐藏的固定会话遭清理。

- 活跃中：复用现有 inProgress 标志，不按账号占用或历史活动推断。
- 未读：直接复用蓝点。点击前记录暂留 ID，蓝点正常确认已读；当前会话继续显示，切到另一会话才移除。状态放在 App 层，手机收起侧栏导致组件卸载后仍有效。取消筛选清除暂留，刷新默认不筛选。
- 中断：只判定最后一回合的 failed/interrupted 与额度错误；成功、进行中、普通手动停止和认证错误不匹配。不使用自动续跑标记推断，不扫描历史中的任意错误。读取未知时展示可重试错误，不当作确定中断。

### 实现与调度边界

新增只读 `POST /codex-api/sidebar-thread-status`，每批最多 100 个 ID。服务端读取原生 `thread/turns/list`，`limit=1`、倒序、`itemsView=notLoaded`；没有全文、工具输出或图片内容。只对已结束的失败/中断回合补充同一回合的流错误，不能把已经成功的回合重新标成额度中断。

独立读取队列最多 4 个真实未结束 RPC，8 秒结束等待后仍保留底层占位，避免重试扩散。缓存最多 2000 项、30 秒，校验目录更新时间，并通过既有历史通知监听在新回合/完成/取消/状态变更时失效；没有增加全局通知订阅。前端取消过期请求并以 revision 拒绝迟到结果。全部、活跃中和未读筛选均不增加状态请求；中断按需加载，未引入定时轮询或账号切换、任务准入、投递与自动续跑状态变更。

原生接口不支持的会话不会退回全文加载；新建且第一条消息尚未提交、历史尚未落盘的会话按非中断处理。来自其他进程的变化依靠目录更新时间、重新连接/窗口聚焦及缓存过期复核；未声称离线页面仍具有实时状态。

### 验证结果

- `pnpm run build` 通过；`pnpm run test:unit`：107 文件通过、716 项通过、1 个原生激活用例按设计跳过。
- `node scripts/audit/0.2.18-sidebar-native.cjs` 通过：真实 CLI + 临时独立 home + localhost 模拟 Responses 服务，2 个本地请求，先额度失败再成功；最终回合 ID 更新，错误清空，返回 items=0。没有真实云端模型调用。脚本清理自身子进程、HTTP 服务和临时 home。
- `node -e "const p=require('node-pty'); console.log('CJS spawn:',typeof p.spawn)"` 返回 function；`node dist-cli/index.js --help` 退出 0。
- Playwright CJS 使用路由拦截建立虚构项目、蓝点、最终中断状态，因此按仓库规则直接使用 Playwright；真实侧栏完成标签单选/取消、未读 A→B 暂留、手机卸载后保留、固定项恢复、项目顺序和折叠状态保留及中英文验证，无 pageerror。
- URL `http://127.0.0.1:4173/` 及虚构 thread 路由；1440×1000、768×1024、375×812 均检查浅/深色，无横向越界，选中文字和边框均为白色。截图原件目录 `/home/Code/codexapp/output/playwright/`，文件名为 `0218-sidebar-filter-<宽度>-<light|dark>.png`。
- 实际 `http://127.0.0.1:59001/`（1440×1000）验证三个标签、点击取消与无页面错误；新状态接口空批次返回 200。截图仅保留标签，见下图。

![59001 实际标签](assets/0.2.18-sidebar-filter/0218-sidebar-filter-candidate-tags.png)

![1440 浅色](assets/0.2.18-sidebar-filter/0218-sidebar-filter-1440-light.png)

![375 深色](assets/0.2.18-sidebar-filter/0218-sidebar-filter-375-dark.png)

[UI 验证摘要](assets/0.2.18-sidebar-filter/ui-verification.json)、[性能摘要](assets/0.2.18-sidebar-filter/profile-summary.json)、[手工测试](../../tests/projects-sidebar-new-chat/sidebar-status-filter.md)。

### 性能与交付

真实 4173 空白测试 home 的启动 profiler：thread/list=1、thread/read=0、skills/list=1、quota=1、providerModels=1，总 API 14.8 KiB，warnings=[]。现有 free-mode/status 约 1.81 秒、provider-models 约 0.97 秒，为当前启动测量，不归因于标签筛选。虚构 UI 验证确认活跃/未读切换不触发新状态请求，中断切换不增加 thread/read；中断 RPC 不创建新会话 worker，不调用 thread/resume 或改变出口选择。未做大型真实历史库长期压测。

构建主 JS 822.28 kB / gzip 260.96 kB，CSS 446.92 kB / gzip 50.21 kB，CLI 942.89 KB；相对原 prepared 基线主 JS 增约 4.09 kB、gzip 增约 1.24 kB。没有新依赖。

59001 由独立 `codexapp-dev-59001.service` 更新，重启前确认所有活动为 0、自动激活关闭。新 PID 为 3328995；home 原地保留，没有复制生产认证数据。4173 验证服务独立由 `codexapp-filter-4173.service` 托管，测试 home 为 `/tmp/codexapp-filter-ui-home`，按普通开发验证约定保留，未来 prepare 阶段应收尾。

生产实查已由用户切至先前 prepared 的 0.2.18，PID 3209859；本轮没有重启或修改生产。**此前 f9a3b9494893 prepared 包不包含筛选功能**；需要发布此追加功能时重新 prepare，不能用旧包代表本次代码。

## 自定义连接会话恢复修复（POE）

### 问题与实现

自定义连接会话在 rollout 中保存 `custom_<storageId>`。切回普通账号后，会话 worker 按当前出口重新创建；原恢复请求却继承历史 provider，而当前 worker 没有该定义，导致 thread/resume 返回 502 / Model provider not found。

恢复和分支请求现在显式传入当前 worker 的 provider；发送前自动恢复也使用同一路径。自定义 worker 直接使用自己的连接 ID，普通 worker 通过原生 config/read 获取实际 provider。保留已有模型参数，不注册其他账号的 provider，不写认证，不更改账号选择、调度准入或全局阻塞规则。

### 已验证

- 59001 原报错会话恢复 HTTP 200。
- 使用现有 POE Responses 连接、模型 assistant，在专用测试会话发送一次短提示，收到 `POE_RESUME_OK`；自定义出口恢复与切回普通账号后的恢复均成功。恢复测试前连接选择，认证文件 SHA-256 前后一致（不记录摘要值或凭据）。
- 真实页面 `http://127.0.0.1:59001/#/thread/01a09568-55f5-7dc2-9322-462a81570673`，Playwright CJS，1440×1000：刷新后回复仍显示，provider 错误 0，pageerror 0；浅/深色截图仅截取测试回复，避免带出账号信息。截图原件 `/home/Code/codexapp/output/playwright/0218-custom-resume-light.png` 和 `0218-custom-resume-dark.png`。
- IPC 回归覆盖普通账号保持运行时，自定义自动化完成、切回普通账号恢复自定义历史、再次选择自定义连接恢复，以及切回普通账号后直接发送触发隐式恢复。原运行会话与认证不变。
- `pnpm exec vitest run`：107 文件、716 项通过，1 项原生激活用例按设计跳过。`pnpm run build` 通过。`node -e "console.log('CJS spawn:',typeof require('node-pty').spawn)"` 返回 function；`node dist-cli/index.js --help` 退出 0。

![POE 浅色刷新验证](assets/0.2.18-custom-resume/0218-custom-resume-light.png)

![POE 深色刷新验证](assets/0.2.18-custom-resume/0218-custom-resume-dark.png)

### 性能与范围

实测一次已加载自定义恢复 43ms，切换普通账号并重建 worker 的恢复 312ms；单次样本，不代表长期延迟分布。自定义恢复不增加 config/read，普通恢复多一个本地 IPC config/read；未增加 HTTP 轮询、全局订阅、历史全文扫描或跨会话扇出，没有缓存失效变化。CLI 943.54 KB，无新依赖，前端不变。

此修复仅更新 59001 开发服务，生产 5900 不变。此前 prepared 包不含本次修复，后续切换须重新 prepare。

### 打包容器补充回归与限制

执行 `pnpm pack --pack-destination /tmp`，用 Node 24 Debian slim 镜像安装 tarball 和 `@openai/codex@0.153.4`，镜像 `codexapp-resume-test:0.2.18`；构建命令：`docker build -t codexapp-resume-test:0.2.18 -f output/0218/Dockerfile.custom-resume /tmp/codexapp-resume-package`。容器均使用独立 `/codex-home`，启动命令 `codexapp --port 4190 --strict-port --no-password --no-open`，只绑定 localhost。

- 4192：无认证，config/read 返回 opencode_zen；切换 OpenRouter 后返回 openrouter_free，HTTP 200。未消耗 OpenRouter 真实请求。
- 4193：损坏 auth.json，config/read 返回 opencode_zen，HTTP 200。
- 4194：合成无效 ChatGPT 认证，发送被接受，但等待 45 秒仍 inProgress、无最终错误。此项未通过，不能声称无效认证错误刷新持久化或该场景重复错误数已验证；未进一步扩大远端重试测试。

上述容器已停止，临时 home 和镜像保留供复核；未挂载生产数据。POE 实际刷新验证通过不等于全部账号失效场景均获验收。打包镜像与最终源码只有恢复请求格式排版差异，无行为差异。
