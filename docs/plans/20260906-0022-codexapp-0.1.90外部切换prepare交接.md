---
title: CodexApp 0.1.90 外部切换 prepare 交接
created: 2026-09-06
updated: 2026-09-06
tags:
  - 开发
  - CodexApp
  - 运维
status: prepare 已完成；生产有活动任务，待外部空闲检查后切换
---

# CodexApp 0.1.90 外部切换 prepare 交接

本轮按用户要求重新执行 prepare，来源为最终封版标签 `release/ui-0.1.90-final` 对应的提交 `500ed282f4540d8a5169bbc6a4ed3ae1b56125d5`。本交接提交仅增加文档，不移动封版标签。

## 准备结果

- 发布时间：2026-09-06 17:13:23 Asia/Shanghai。
- 发布目录：`/home/docker/codexapp-releases/codexapp-0.1.90-500ed282f454-20260906-171234`。
- 包：`/home/docker/codexapp-releases/codexapp-0.1.90-500ed282f454-20260906-171234/codexapp-0.1.90.tgz`。
- SHA-256：`0b4836cff92948972a7a943af3d95e770694c19c0248b5687c725d8ea3c1d1eb`，与 0021 最终包完全一致。
- `latest-prepared` 已更新到上述目录；旧包保留。
- 构建和类型检查通过，CLI help 通过；node-pty CJS 导出、实际 PTY 输出和退出码 0 均通过。dist/dist-cli 共 29 个文件与 59001 当前候选哈希一致。
- 59001 活动任务/排队/待审批均为 0。5900 仍运行 0.1.89，PID 87226 未重启。
- `bash scripts/codexapp-release-switch.sh check latest` 返回 3：`activeTurns=1, queued=0, pendingApprovals=0`。prepare 成功，但当前不能宣称生产已经空闲。
- 未执行 activate、未导入测试认证、未修改生产认证或状态。业务性能与浏览器回归沿用 [0021 最终封版](20260906-0021-codexapp-0.1.90白屏修复与最终封版.md)，因为安装包逐字节相同。

原始日志：`/home/Code/codexapp/output/0190-external-prepare/`，包含 prepare.log、check.log、artifacts.json。

## 外部终端操作

先结束当前 Codex 任务，关闭 CodexApp 标签页，并确认没有其他主机 CLI 任务。用本机独立 SSH/终端执行以下命令。显式使用本次目录，避免将来 latest 被另一次 prepare 改写。check 若报告忙碌，应等待任务结束；不要绕过。

```bash
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh check /home/docker/codexapp-releases/codexapp-0.1.90-500ed282f454-20260906-171234 &&
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh activate /home/docker/codexapp-releases/codexapp-0.1.90-500ed282f454-20260906-171234 --confirm-idle
```

切换脚本会再次检查空闲状态并创建事务/回滚记录。完成后检查：

```bash
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh status
curl -fsS http://127.0.0.1:5900/ >/dev/null
```

预期 active_release 指向本次 0.1.90 发布目录。浏览器普通刷新应直接显示 UI，核对自动化、Goal 和既有会话；生产自然周期执行与物理设备验收按 0021 继续。

## 回滚

仅在实际切换后发现问题且任务已空闲时，从外部终端执行：

```bash
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh rollback --confirm-idle
```

依照本次 activate 创建的事务恢复前一代码版本，不手动删除状态卷或复制候选认证。
