---
title: CodexApp 0.2.9 prepare 交接
created: 2026-09-07
updated: 2026-09-07
tags: [开发, CodexApp, 发布]
status: prepare 完成，未激活
---

# CodexApp 0.2.9 prepare 交接

用户授权 prepare 0.2.9。本次从干净的 `feature/0.2.9-final-audit`／`dc2f41030065` 构建并准备版本目录；没有修改应用源码、推送、合并或激活生产。

## 精确产物

- 发布目录：`/home/docker/codexapp-releases/codexapp-0.2.9-dc2f41030065-20260907-092836`。
- 打包文件：该目录下 `codexapp-0.2.9.tgz`；SHA256 `05762672359d8b0a82c23e1218ff2f867ce3526345644c65fc057ffebb743c0a`，与已验收候选包一致。
- CLIProxyAPI 7.2.152 位于该目录的 `api-proxy-component/`，二进制 SHA256 `fc9b13ecda255129b7c452a8c0a84bd0669de0b546d0919f5566cff5397c0c23`。搬运时保留整个发布目录。
- `latest-prepared` 已指向上述目录，ready 标记存在；原发布和候选保留。

## 验证与修复

构建含 vue-tsc，通过。发布目录 dist/dist-cli 共 30 个文件与运行中的 59001 一致；CJS require 实际 CLI help、node-pty 导出及真实 PTY 输出 `PREPARED_PTY_OK`／退出 0 均通过。

首次安装因本机 Node 头文件位置与容器不同，node-pty 可选依赖构建失败并被 npm 移除。验证发现后临时撤下 ready 标记，仅在新发布目录使用本机头文件 `/root/.nvm/versions/node/v24.19.0` 重新安装运行依赖，node-gyp 构建成功；完整复验通过后恢复 ready 标记。未修改项目依赖声明，未放开其他安装脚本。

修复命令仅针对本次新目录：

```bash
npm_config_nodedir=/root/.nvm/versions/node/v24.19.0 npm --prefix /home/docker/codexapp-releases/codexapp-0.2.9-dc2f41030065-20260907-092836 install --omit=dev --no-package-lock --no-save --include=optional --foreground-scripts
```

使用全新临时 HOME／CODEX_HOME 在 59015 启动真实发布包：API 出口组件已安装、默认关闭，无 key 的 `/v1/models` 返回 401。测试进程及临时状态已清理，59015 已无监听；未复制生产或候选认证。

性能与功能沿用一致产物的 [0.2.9 验收报告](20260907-0008-codexapp-0.2系列开发报告与候选交付.md)。本次没有应用行为变更，不重复 UI 或全量单测；增加的是实际宿主机安装和原生模块验证。

## 当前边界

最终 `check` 退出 3：activeTurns=1、queued=0、pendingApprovals=0、apiConnections=0、apiActiveRequests=0；后台终端检查因已有活动回合而跳过，不能记作后台数量 0。

check 同时提示 systemd 磁盘配置与已加载状态存在差异。本次没有执行 daemon-reload；实际进程 PID、启动时间和 ExecStart 与 prepare 前逐字节一致。生产仍为 0.2.0／PID `3456650`，启动于 `2026-09-06 22:21:46 CST`。59001 仍运行已验收的 0.2.9 镜像 `c444771e20da`。

原始日志：`/home/Code/codexapp/output/0209-prepare/`。结构化结果见 [prepare 验证](evidence/0.2.9-prepare-verification.json)。本文和验证数据已同步 Obsidian 并核对一致。

## 后续外部终端命令

仅在用户另行决定上线、当前任务结束，并核对上述 systemd 配置差异后使用。本次 prepare 不代表已执行或已授权本会话 activate。使用精确目录，不依赖未来可能变化的 latest。

```bash
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh check /home/docker/codexapp-releases/codexapp-0.2.9-dc2f41030065-20260907-092836 &&
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh activate /home/docker/codexapp-releases/codexapp-0.2.9-dc2f41030065-20260907-092836 --confirm-idle
```

激活后的状态核对及必要回滚：

```bash
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh status
# 仅在已激活该版本、确定需要回滚且共同空闲后执行：
sudo bash /home/Code/codexapp/scripts/codexapp-release-switch.sh rollback --confirm-idle
```

rollback 根据当前切换事务恢复上一个服务定义与代码，不应直接改动生产账号状态。不要在尚未激活 0.2.9 时运行上述 rollback，以免回滚其他已有切换事务。
