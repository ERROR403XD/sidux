---
title: CodexApp 0.2.18 追加修复切换交接
created: 2026-09-12
status: 已 prepare，未切换
---

# 0.2.18 追加修复切换交接

本次 prepare 固定最新运行代码 `6ad0e99242c8`，替代旧交接中的候选路径；旧审阅基线文档保留其历史含义。没有执行 activate，没有重启生产或修改认证配置。

## 精确制品

- 路径：`/home/docker/codexapp-releases/codexapp-0.2.18-6ad0e99242c8-20260912-221253`
- 完成：2026-09-12 22:13:39 +08:00。
- tarball：`/tmp/codexapp-0.2.18.tgz`，SHA-256 `b19b59cbd76406af42b943d15e612b144aa02d5b77f60606f364431353a1040f`。临时归档可能被下次 prepare 覆盖，切换以精确目录为准。
- 生产仍为 `codexapp-0.2.18-f9a3b9494893-20260912-162826`，PID 3209859。
- 新增内容：账号切换后的会话恢复与模型偏好、自定义连接恢复、侧边栏筛选及当前行保留、额度中断忽略、运行中分支与即时侧边栏显示、无项目欢迎区、操作 Toast 的位置/动画/进度条，以及正文后斜杠搜索。详见开发验收文档。

## 验证

- `pnpm exec vitest run`：110 文件通过、1 文件跳过；738 项通过、1 项跳过，27.76 秒。
- `scripts/codexapp-release-switch.sh prepare`：类型检查、前端与 CLI 构建、pack、独立目录依赖安装、API 组件校验、CLI `--help` 通过。
- 逐文件比较 `dist` 和 `dist-cli`：27 个文件与 prepared 包一致。
- CJS：`require('<release>/node_modules/node-pty')` 的 spawn 为 function；实际启动 `/bin/sh -c 'printf PREPARED_PTY_OK'`，收到标记、退出 0。
- 安装报告 0 个已知漏洞；安装脚本策略警告不影响上述实际 PTY 验证。这不等于审阅问题已修复。
- 性能：主 JS 827.16 kB / gzip 262.93 kB，CSS 449.45 kB / gzip 50.79 kB。相比初次 prepare 的主 JS 818.19 kB，增加约 1.1%。近期 UI 修改无新依赖；Toast 最多 3 条，CSS 倒计时无逐帧 JS 和新增请求。未追加大历史库压测或新一轮真实远端账号请求；沿用此前记录的 59001 真账号 A→B→A 验证及浅/深色 UI 证据。
- 日志：`/tmp/0218-prepare-final.log`、`/tmp/0218-prepare-final-tests.log`、`/tmp/0218-prepare-final-check.log`。

## 当前空闲检查

59001：liveExecutions、activeTurns、queued、pendingApprovals、API 活动、backgroundThreads 全部为 0，loadedThreads=1。

5900：精确路径 `check` 返回 3，liveExecutions=1、activeTurns=1，其余队列/审批/API 为 0。活跃线程 `01a092ef-b5fb-7010-8cb0-430f2d24965d` 与当前执行环境 CODEX_THREAD_ID 一致，确认为本次 prepare 会话自身；不是新增测试残留。backgroundThreads=skipped-busy，尚未完成生产后台终端全量枚举，不能记为 0。

额外读取两端 `/codex-api/api-proxy/activation`：enabled=false、nextAt=null、runs 为空。此前 SCH-01 的定时激活门禁缺口没有修复；当前设置关闭，切换前应保持关闭并重新核对。其他历史审阅发现仍以审阅报告为准，本次 prepare 不代表安全问题已全部关闭。

## 切换步骤

结束本回合并关闭 5900/59001 页面，在独立宿主机终端确认没有其他直接 Codex CLI 任务；重新核对账号定时激活仍关闭，然后执行：

```bash
cd /home/Code/codexapp
scripts/codexapp-release-switch.sh check /home/docker/codexapp-releases/codexapp-0.2.18-6ad0e99242c8-20260912-221253
```

仅当检查通过、后台终端枚举已完成且确认执行切换时：

```bash
scripts/codexapp-release-switch.sh activate /home/docker/codexapp-releases/codexapp-0.2.18-6ad0e99242c8-20260912-221253 --confirm-idle
scripts/codexapp-release-switch.sh status
```

核对 active_release 精确路径、5900 页面和原账号/会话，再进行一条明确的验收对话。59001 是独立 systemd 开发服务，prepare 未停止它，也不会导入其账号池。不要将开发目录复制到生产 CODEX_HOME。

需要回滚时，结束活动、关闭页面，在独立终端执行 `scripts/codexapp-release-switch.sh rollback --confirm-idle`；保留此前发布目录与事务快照，不手工覆盖历史认证。
