# 0.2.16：会话完成列表与线程菜单

前提：59001 独立候选；使用两个浏览器上下文。自动测试用合成完成事件，不消耗真实模型或重置额度。

操作：
1. 完成列表为空时加载历史线程，令历史 updatedAt 晚于当前时间，再刷新/换另一终端登录。
2. 触发一个新回合完成，在两个终端检查蓝点，刷新其中一个终端。
3. 点击对应会话，核对两个终端的蓝点清除；再次刷新，蓝点不补回。
4. 同一线程再完成一个回合后，以旧回合 token 提交清除，核对新的蓝点仍保留；重复投递已清除回合的完成事件，不再次加点。
5. 加载其他分页/执行静默历史刷新，确认这些读取不新增或清除完成列表；右键线程菜单检查无“复制路径”“复制聊天”。
6. 在浅深主题分别切换自动/深色/浅色图标；自动应为显示器 monitor，三个状态图标和按钮宽度一致。

预期：服务器在 CODEX_HOME/codexapp-thread-completions-v1.json 维护完成列表，从空文件开始，不导入旧 localStorage 或扫描历史。浏览器连接只读取一次快照，后续增删通过事件同步；断线重连重新取快照。点击携带当前完成 token，旧点击不清除新回合。当前选中/运行中线程沿用侧栏指示优先级，不显示蓝点；后台读取不算点击。

清理：关闭合成测试上下文；自动测试使用临时 CODEX_HOME 并自行删除。不要清空真实候选状态来运行测试。

## 2026-09-11 正在查看的会话完成时自动已读

准备：4173/59001，两个会话 A/B；使用合成完成通知，无需真实模型。

操作与预期：
1. 页面可见并打开 A，发送 A 的完成通知，随后切到 B。A 无蓝点；服务器收到携带 A 当前 token 的一次已读确认，重复通知不重复提交正在等待的确认。
2. 打开 A 但将浏览器标签页置于后台，或切到设置/自动化页，再发送 A 完成通知。保留未读，不自动提交确认。
3. 正在看 A 时让 B 完成，B 正常显示蓝点。
4. 延迟 A 的旧回合已读响应，切到 B 后再让 A 新回合完成，再放行旧响应。新回合蓝点保留。
5. 刷新页面或另一终端重新读取完成列表，已经确认的 token 不恢复蓝点；确认请求失败时保留记录供后续点击重试。

验证：`pnpm exec vitest run src/composables/useDesktopState.test.ts`，59 项通过，包含以上前四项回归。前端类型检查及构建通过。

性能：只在完成事件且当前会话路由/页面可见时提交一次 POST；使用 Map 合并同 token 的在途确认，结束后清理，无额外轮询或全量会话读取。主 JS 相对上一提交增加约 0.30 kB（gzip 约 0.14 kB）；未测量跨设备网络延迟。

清理/回滚：关闭合成验证页；回退本次前端提交，不清空真实完成列表。

2026-09-11：跟随系统主题使用显示器轮廓图标，手动浅色/深色继续使用太阳/月亮。1440 与手机侧栏展开时检查图形、按钮尺寸及标题，循环切换后恢复自动。仅 SVG 替换，无资源请求或运行时逻辑变更。
