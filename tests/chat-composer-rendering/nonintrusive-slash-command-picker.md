# 0.1.90：斜杠命令选择器

前提：4173 使用隔离的 `CODEX_HOME`，或在 59001 内部测试会话操作。至少准备一个虚构技能和一个保存的提示词。不要向真实业务会话发送测试文本。

1. 在完全空的输入框中键入 `/`；空白后、换行后和已有正文中不打开。预期浮窗靠近输入框，焦点仍在 textarea，显示命令名、描述和类别；默认没有选中项。
2. 连续输入英文名称或中文描述。预期本地过滤，没有按键触发的 HTTP 请求。每次修改查询均清除键盘选择。
3. 第一次按 ↓ 选中第一项，↑ 选中最后一项；继续按键循环选择。按 Enter 仅执行已选项；鼠标点击也能执行，但悬停不会成为键盘选中项。
4. 直接输入 `/plan` 并按平时的发送键，不使用方向键。预期发送原文字；不强制解释为命令。分别验证 Enter 发送和 Ctrl/Cmd+Enter 发送设置，Shift+Enter、Tab 保持原语义。
5. 测试 `/tmp/path`、`https://example.test/a/b`、日期、代码块及正文斜杠。预期不吞字符。粘贴包含斜杠的文本不主动打开窗口。
6. 按 Esc 或点击外部后继续输入当前 token。预期浮窗保持收起、文字保留；清空草稿后重新键入 `/` 可重新打开。执行命令后再次输入 `/` 也能打开。
7. 使用中文输入法组合输入，特别在候选确认时按 Enter。预期不发送消息、不选择命令；组合结束后继续搜索。
8. 选择 `/plan`、`/default` 切换已有模式；选择 `/model`、`/skills` 打开现有选择器。取消子菜单时保留斜杠文本，选择时才替换对应 token。期间改动草稿不得误删新文字。
9. 从空草稿输入 `/prompts:weekly` 并选择保存的提示词，预期替换命令 token；已有正文时不打开命令窗口，Ctrl/Cmd+Z 可撤销该文字替换；选择 `/skillname` 附加技能路径，不自动发送，保留已有附件与正文。
10. 展开输入框后重复；验证 1440×900、375×812、768×1024 的浅色/深色主题。预期窗口可见、列表可滚动，发送按钮不被长技能名挤出屏幕。
11. 使用 100/1000 个候选检查持续输入及方向键定位末项。预期只渲染附近的有限行；搜索 p95 小于 16ms，不出现逐键网络请求。

自动验证：`pnpm run test:unit` 包含 token 边界、键盘选择、中文搜索、粘贴与关闭抑制测试。实际 UI 截图和请求/性能记录位于 `output/playwright/0190-*`。

清理/回退：清空虚构会话草稿和测试附件；关闭测试会话。需要回退时切换保留的 0.1.89 候选镜像，不修改生产认证或历史会话。

## 0.1.90 封测修订：实用命令目录

目录统一维护在 composerCommands.ts，共 22 项内置命令：/plan、/default、/model、/skills、/goal、/compact、/new、/rename、/fork、/review、/diff、/status、/copy、/resume、/apps、/plugins、/mcp、/automations、/export、/help、/mention、/init。可用技能与保存的提示词继续动态补入。

前提：使用隔离内部会话；需要模拟 RPC/剪贴板/下载时使用 Playwright，禁止将虚构请求发到生产。

1. /help 应列出所有命令及用途；输入 / 持续搜索、无默认选择、普通斜杠、Esc、粘贴和中文输入法契约保持不变。
2. /goal 打开目标窗口，填写目标和可选 Token 预算。保存、暂停、继续、清除分别调用 thread/goal 接口；重新打开恢复目标及统计。新会话允许先创建线程再保存原生目标，不能将 `/goal` 伪装为普通提示词，也不能额外提交一个重复的 turn/start。原生目标激活会自动开始执行。
3. /compact 在空闲线程调用 thread/compact/start；提示“已请求压缩”，进度在会话查看，不能因请求返回空对象就报告完成。忙碌时显示原因并暂不执行。
4. /rename 修改名称；/fork 创建独立分支；/review 发起当前未提交变更的审查；/diff 打开现有差异面板。无会话或忙碌时应有对应限制和错误提示。
5. /copy 只复制最近已完成的助手回复，跳过后来的进行中输出；/export 下载当前已加载内容的 Markdown。
6. /new、/resume、/apps、/plugins、/mcp、/automations 分别复用新建、搜索和对应页面入口，不新建重复设置体系。
7. /mention 插入 @ 并打开文件选择；/init 插入 AGENTS.md 初始化指令供用户确认发送，不擅自写文件。忙碌时实际发送这些草稿仍应排队。
8. 取消需要填写内容的命令窗口保留原 token；操作成功才消费 token。浅深色、375×812、768×1024 下弹窗可读可滚动；Esc、Tab 焦点和关闭后焦点恢复正常。

回归：`pnpm exec vitest run src/components/content/composerCommands.test.ts src/api/threadCommands.test.ts`；检查选择器搜索没有逐键 API 请求，命令窗口按需懒加载，完整历史/完整线程请求只能由相应明确操作触发。

清理：测试目标暂停/清除，等待已开始的回合结束；移除虚构队列和临时导出。生产版本与认证不参与此验收。

预算单位修订：未写单位按 M（百万 tokens），支持 1.5M、0.01B（十亿 tokens）及小写单位。验证输入 1 实际 RPC tokenBudget=1000000，0.003M=3000；重开后显示 0.003M。留空为 null，无自动默认预算；非正数、非整数 token 或超出安全整数范围应在提交前报错。

## 持续目标入口、会话卡片与侧边栏标记

前提：4173 虚构线程或 59001 内部线程；保存一个暂停的 Goal，另备一个无 Goal 的线程。

1. 点击输入框左下“＋ → 持续目标”，应打开与 /goal 相同的管理窗口，输入框中的原草稿保持完整。
2. 新会话保存 Goal 后进入对应线程，标题使用目标摘要；会话显示目标正文、状态、用量/预算和“管理目标”，原生自动推进且不额外发送伪造的用户消息。
3. 已有、暂停和完成的 Goal 均显示卡片；侧边栏显示与闪电同风格的目标图标，悬停说明 Goal 状态。不同侧栏分组、置顶和搜索结果都可见。
4. 刷新页面、重新打开会话，卡片与标记应恢复；暂停/继续状态同步更新。清除 Goal 后卡片和标记消失，会话记录保留。
5. 浅/深主题及 375×812、768×1024 下检查卡片不遮盖消息或输入框，长目标在卡片内部滚动；检查展开输入框后的菜单位置。

性能：线程加载后批量读目标；每个 HTTP 最多 100 个 ID，服务器原生 goal/get 并发最多 4，同一实例共享 30 秒缓存及通知失效。目标读取不能阻塞 thread/list/resume，不读取完整历史，不扫描本机数据库文件。记录冷/热批次耗时和 API 数量。

清理：清除虚构 Goal，等待或停止其已经开始的测试回合，移除测试草稿，不修改用户的真实目标。

## 0.2.0 Goal 模型与推理强度

前置：隔离 59001、支持 `thread/settings/update` 的 Codex CLI；准备暂停的测试目标，模型目录含不同强度能力。

1. 打开 Goal 弹窗，检查模型搜索和推理强度使用与自动化相同的 AppSelect；首次读取会话实际保存的 model/reasoningEffort。
2. 选择 Astra/ultra，保存暂停目标。请求应先 `thread/settings/update` 再 `thread/goal/set`，目标保持暂停；重开及刷新后选择保持。
3. 继续目标也先应用设置；暂停和清除不提交模型设置。模拟设置失败时显示错误，不能继续激活目标。
4. 切换到不支持当前强度的模型，显示能力提示并禁止保存，重新选择有效强度后恢复。默认强度使用所选模型公布的默认值。
5. 在新会话创建目标，确认先建线程、设置模型/强度，再设置 Goal；不添加伪造的用户消息。当前已开始回合不受设置变更影响。
6. 1440×900、375×812、768×1024 浅深主题检查弹窗、菜单、滚动与无横向溢出。

性能：复用已有模型目录；打开已有 Goal 仅新增一次 `thread/read(includeTurns:false)`，与 goal/get 并发，不加载历史、无新轮询。清理：清除测试目标，归档测试线程；恢复用户实际配置，不操作生产。

## 0.2.9 说明收敛

打开 `/goal`，默认突出目标、模型、预算和操作；“预算与暂停说明”默认折叠，展开后仍可查看预算可能超出和停止当前回合的区别。检查浅深主题和 375/768 宽度，保存/暂停/继续规则不变。`/help` 仍列出实际可用命令，搜索任务仍明确正文索引范围和摘录限制。关闭窗口不应提交草稿。

## 0.2.18 已有正文后的斜杠搜索

前提：隔离 4173/59001，打开新会话输入框。

1. 输入“已有内容/plan”、`hello/plan`，以及多行正文后紧接 `/plan`：无需额外空格即可弹出命令搜索。选择 /plan 后仅移除命令片段，原有中文、英文、换行完整保留。
2. 菜单底部保留键盘操作提示，删除“也可直接输入文字”。
3. URL、绝对路径/显式相对路径的后续斜杠、代码块和行内代码不作为命令；未选择命令时原文本仍按普通输入处理。粘贴不自动展开；Esc 关闭后编辑同一 token 不强制重开。
4. 1440×1000、375×812、768×1024 浅/深色检查真实菜单，无横向溢出。纯客户端字符串匹配不新增请求、轮询或改变消息发送协议。

清理：清除测试草稿，不发送真实会话消息；回退前端文件即可。

## 0.2.19：仅草稿开头触发

- 前置：当前候选服务，浅色与深色主题；使用未发送的草稿。
- 操作：空输入框键入 `/`、`/plan`；删除后键入 `正文/plan`、`正文 /`、换行后 `/`、`/plan /`；测试粘贴、Esc、方向键及输入法。
- 预期：仅从字符 0 开始的命令打开搜索；正文、空白后及后续行不打开。Esc、粘贴不自动打开，Enter 的原有发送/选择规则不变。此项取代 0.2.18 的“已有正文后输入 /”预期。
- 清理：清空草稿，无需发送请求；恢复主题。

- 空结果文案：输入开头的不存在命令，中文显示“无匹配命令”，英文显示“No matching commands”；不再显示继续输入/发送说明。


## 0.2.19 封测：空草稿激活与展开定位

前置：4173 当前工作树，真实输入框；测试只改变浏览器草稿，不执行模型或账号操作。
操作：运行 `node scripts/test-expanded-composer-ui.cjs`。覆盖已有单词/中文/空白/换行，在文本最前面插入 `/`；恢复或粘贴 `/plan`；从空输入键入 `/pl` 并用方向键/Enter 选择；加入正文后回到开头。展开后保持菜单打开并连续调整窗口。
预期：仅从空草稿键入 `/` 开始搜索，之后可继续输入查询；粘贴、恢复和已有正文不激活。展开菜单在 textarea 中间稍下方，不覆盖首行；普通输入继续贴近输入区。关闭后继续打字不会重新打开，清空后新输入才打开。
验证：1440×1000 → 1440×620 → 1100×620 → 768×1024 → 375×812 → 375×500 → 1440×1000，浅深各一轮；菜单、控件不越界，焦点/草稿/选区保留，无执行 RPC。截图 `output/playwright/0219-sealed-composer-{1440,768,375}-{light,dark}.png`，JSON 在 `output/0219-final/sealed-composer-ui.json`。
性能：只在打开时观察 textarea 和浮层尺寸，resize/scroll 合并到单个 RAF，关闭后断开 observer、监听和 RAF；继续仅渲染最多 12 个命令行，无逐键网络请求。清理：脚本清空临时草稿并关闭浏览器。
