### 最小问题报告（0.2.18 / R01）

#### 前置条件
- 4173 为当前工作树；使用隔离 CODEX_HOME。准备浅色、深色主题，桌面和手机视口。
- 用浏览器路由拦截使 `/codex-api/skills-hub?*` 返回 500 和合成错误，不修改真实服务。

#### 操作
1. 进入应用的技能页，点击错误旁的反馈入口。
2. 检查“问题报告”弹窗，编辑复现步骤并复制；关闭后重新打开。
3. 查看当前项目 Issues 链接目标，不实际提交。
4. 测试剪贴板不可用时仍能选中报告文本，重复明暗主题检查。

#### 预期
- 点击后只打开应用内预览；不会打开邮件客户端、自动打开外站或发送报告。
- 默认报告仅包含版本、时间、页面类别、主题、语言、视口、错误类别及 HTTP 状态；不包含会话正文、错误原文、完整 URL、目录、浏览器存储或凭据。
- 报告最多含 12 条诊断。Issues 指向 `https://github.com/ERROR403XD/codexapp/issues`，报告不会通过 URL 参数传出。
- 复制失败时提供可选中的文本和明确提示。弹窗使用共享组件，Esc、焦点返回和明暗主题正确。

#### 清理
关闭测试浏览器，移除路由拦截；不要提交测试报告。旧邮件采集行为已由本条替代。


## 0.2.18 操作提示关闭范围

前提：隔离开发页；准备“需建立分支”等操作不满足条件的通知，以及 RPC 502、目标读取失败样本。

1. 操作提示仍有 ×，点击立即关闭，无需刷新；同一内容的普通重新渲染不重新打开。
2. RPC 502、目标读取、CLI 缺失、模型目录、历史/进程读取等系统错误保持原有展示，不因本次操作通知需求新增关闭按钮。
3. 历史回合错误保留，额度“忽略”功能不受影响；不混同关闭操作通知与忽略额度错误。

清理：撤销测试故障拦截，不更改服务端错误或执行状态。

## 0.2.18 用户操作 Toast 与持续错误边界

前提：隔离 4173/59001，准备虚构账号、会话和目标；使用浅/深色主题。不得通过真实失败消息猜测类别，应按触发路径验证。

1. 用户创建分支、保存目标、暂停/继续/清除目标、切换账号、保存/移除自动化：成功后出现一次轻量成功通知；账号切换成功依据接口结果，不能仅凭点击显示成功。
2. 对上述操作注入 HTTP 502 或校验失败：操作失败使用 Toast，显示失败原因，原业务保护和数据状态仍有效。关闭后再次执行同一失败操作，应再次通知。
3. 对自动读取目标/队列/账号/模型目录注入失败：原位置持续显示错误与重试入口，不转成 Toast。对历史回合注入上游错误：错误仍位于对话中，不自动消失。
4. 成功通知 3 秒、失败通知 6 秒关闭；悬停或键盘焦点进入时暂停，离开后继续剩余时间；× 可关闭，同类重复通知合并，最多 3 条，不抢输入焦点。切换会话后清除旧会话的操作通知。
5. 在 1440×1000、375×812、768×1024 浅/深色检查右上角/手机顶部通知，无横向溢出；不占用输入框布局，不产生新的请求、轮询或无限计时器。

证据脚本：output/0218/operation-toast-ui.cjs、operation-toast-success-ui.cjs；均用本地接口拦截，不调用真实上游。清理只处理测试 fixture，回退前端代码不更改账号、目标、额度忽略或队列状态。

### Toast 顶边避让工具栏

前提：隔离 4173/59001，触发操作通知。在 1440×1000、375×812、768×1024 浅/深色切换视口并检查：通知容器顶边等于 Git／新建会话所在 `.content-header` 的底边（误差小于 1px），工具栏按钮不被遮挡。多个通知向下堆叠；既有关闭和自动消失行为不变。回退只恢复前端布局。

正文检查：通知直接显示“请先创建分支。”等具体内容，不附加“操作提示：”或“提示：”标题。验收 fixture 同样使用纯正文，避免把测试前缀误认为产品样式。

### Toast 动画与倒计时条

触发成功/失败通知：底部 2px 细条分别按 3 秒/6 秒从满到空递减。悬停和键盘聚焦暂停条与关闭计时，离开后从原位置继续；× 可立即关闭。通知轻微横向淡入淡出，删除一条后剩余条平滑补位；启用减少动态效果时禁用位移动画。检查桌面、手机、平板浅/深色，不新增定时轮询，不影响工具栏避让。
