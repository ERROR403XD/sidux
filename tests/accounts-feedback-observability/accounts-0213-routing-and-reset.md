### 0.2.13 账号路由与重置确认

前置：隔离候选59001；路由验证使用 `src/server/apiProxy/gateway.test.ts` 假上游，禁止真实重置。

1. 后台刷新A，同时请求 `/v1/responses`、`/v1/chat/completions`：都成功。
2. 主会话忙时切换账号：空闲检查直接拒绝，不先排空API出口。
3. 两个Key分别建立流式请求；将卡住的Key改绑B：它的旧连接结束，新请求成功，另一Key仍忙且连接未断。
4. 模拟HTTP浏览器没有 `crypto.randomUUID`，点击假重置机会并确认：请求包含UUID、选定账号和机会ID、confirmed=true；仅一次POST。检查模拟成功提示。
5. 检查账号卡页脚“全局设置”，运行时中文“账户”统一为“账号”。

预期：固定账号Key不受其他账号刷新/切换影响；登录等待期间不阻止主会话提交，实际主凭据更换保留空闲门禁。
清理：测试临时目录自动删除；浏览器拦截 `/accounts/reset-credit`，不得转发真实请求。
