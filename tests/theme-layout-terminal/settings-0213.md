### 0.2.13 模型强度整合及503恢复

前置：4173独立CODEX_HOME，Playwright脚本 `output/0213/ui.cjs`；浅深主题及1440×900、768×1024、375×812。

1. 第一次模型目录请求模拟503，下一次成功；无需刷新，错误消失，目录可选。
2. 点击模型强度控件，选择支持的强度，进入模型列表选择Sol；刷新页面仍选Sol。
3. 模型未公布的强度不得作为滑块档位；“恢复模型默认推理强度”回到目录默认。
4. `output/0213/new-thread.cjs` 创建假线程，再令首次发送返回503：页面进入已创建线程，文本草稿保留，无需刷新找线程。
5. `output/0213/mcp.cjs` 在MCP页面发送200条重复启动状态：最多增加1次查询，已有列表不进入加载闪烁。

预期：弹层不溢出，浅深表面一致，没有页面异常；成功截图保存于output/playwright/0213-*.png。
清理：关闭浏览器；保留4173；假线程、目录和发送均被拦截，不写真实会话。
