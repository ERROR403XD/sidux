# 0.2.11 设置与账号区域

前置：使用独立 CODEX_HOME 的候选，准备两个测试账号及一个有草稿的会话；同时覆盖无账号和无用量状态。

1. 在侧栏展开、折叠及移动端检查展开侧栏工具顺序：侧栏、主题、设置、搜索；折叠仅保留展开按钮。新建在内容右上角 Git 右侧，自动主题为太阳+A。
2. 打开设置，检查账号与连接、通用、外观、输入与发送、集成、关于；逐项修改旧偏好并刷新，确认原保存语义和配置保留，返回会话草稿不丢失。
3. 循环主题，检查深色、浅色、系统图标及系统变化；两处设置保持一致。
4. 左下角上下文和账号用量独立显示；打开账号弹层，检查长名称、当前标识、双窗口额度、更多菜单、双版本；刷新列表不重载会话。测试添加、重新认证、刷新额度、切换限制和移除确认。
5. 检查“关于”版本与实际运行产物一致，能力文案没有“尚未接入”的旧说明；失败状态可见，重复打开不重复检测。

视口：1440×900、375×812、768×1024，全部浅/深主题；保存实际弹层和设置页截图。关闭子菜单后焦点正确，登录对话框不被父菜单的 Esc 提前关闭。

回滚/清理：恢复测试偏好；只移除本轮新增的测试账号（不可移除激活账号），不清空已有账号/会话数据。组件回滚不修改运行凭据。

阶段结果：2026-09-08 类型检查及构建通过，公开 CJS help 入口退出 0。fixture 页面六组浅深视口验证设置分区、刷新路由和账号弹层；真实第二账号授权/切换另行核验，不把 fixture 当作认证验收。

## Fast 与加号（UI-01 / UI-02）

在支持 Fast 的测试模型下开关 Fast，比较开关前后模型和 Fast 按钮的 x/width；只有按钮内部闪电着色，模型旁没有额外图标。检查加号视觉中心与 Fast 中心相差不超过 2px，点击加号附件菜单正常。桌面/手机/平板各浅深主题通过；证据 `output/0211/toolbar.json`。清理：恢复原速度设置。

## 会话默认值与记忆

前置：模型目录可用，至少两个由本版 WebUI 创建的会话，另准备自动化/API 创建的会话。设置页配置默认模型、思考强度、Fast 后刷新，控件值保留。

1. 新建 WebUI 会话，核对初始控件和实际投递参数；会话 A/B 分别设置不同模型/强度/Fast，切换和刷新分别恢复。
2. 关闭记忆，当前输入区保持不变，重新进入使用默认值；此时手动修改仍用于下一次发送，重新进入又恢复默认。开启记忆可恢复关闭前的记录。
3. 打开后台或未知来源会话，不能自动套用新默认值；更改默认值不改变自动化/API 定义或在途请求，不产生 config/batchWrite。
4. 模型被移除或能力变化，显示不可用状态，不默默跨来源替换。模拟浏览器存储写入失败，保存错误可见，原保存值不丢失。
5. 页面不出现“仅作用于 WebUI…”等入口范围说明或 tooltip。检查浅深主题及移动视口。

回滚：撤回会话偏好接入，保留原 localStorage 与新增 `codexapp.web-conversation-preferences.v1`，无须修改 Codex 全局配置。性能：偏好读取每页面实例一次，切换本地 O(1) 查找，不发额外模型/历史请求；保存随显式操作写入一次。

## 59001 导航与额度精简

前置：两个受控账号、项目会话和 `Documents/Codex/YYYY-MM-DD/<任务>` 无项目会话。桌面 1440×900、手机 375×812、平板 768×1024，各浅深主题。

1. 项目会话点右上角新建，目录继承当前会话；无项目会话、设置、自动化路由新建，目录为空，不继承上次项目。
2. 左下角常驻显示更新时刻与两行额度，5小时/7天限额、条形、剩余百分比、重置时刻；重置时间悬停查看完整日期。切换全局 UTC 与上海，更新时间、重置时刻立即换算。
3. 已用 0/20/40/60/80% 对应蓝/绿/黄/橙/红；条形宽度表达剩余，用掉部分仍有斜纹边框。100% 耗尽时轨道仍可见。
4. 当前账号有蓝色描边，正常无就绪字样；掉登录与刷新失败必须显示错误。手动刷新列表对每个账号各刷新一次，同时显示新等级；重新读取仍为新等级。
5. 设置无“到点仅对…”及“应用于本浏览器…”说明长句。

验收脚本：`output/0211-refine/ui.cjs`、`edge.cjs`；6组视口主题与时区/错误/项目归属均通过。回滚：恢复偏好，保留账号及命名卷；受控 fixture 不消耗模型额度，不代表真实套餐升级实测。

### 额度日期标签

前置：全局时区上海，系统时间2026-09-08 23:00，5小时重置2026-09-09 01:00、7天重置2026-09-14 01:00。检查额度卡片/左下角分别显示“明天 01:00”“周一 01:00”，无月/日；时间列同宽，375/768/1440视口浅深不换行或溢出。UTC切换后5小时应为“今天 17:00”；跨夏令时按当地日历判断明天。过午夜后共享本地分钟时钟重新计算，不额外读取额度。恢复时区及测试时钟，真实账号不发送测试消息。
